# Secrets / local config (keep .env.example as the template)
.env
bridge/.env

# Mod-written runtime config (regenerated by the mod at startup)
config.json
bridge/config.json

# Transcripts / logs
*.log
chat.log
bridge/transcript*.txt

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Build artifacts (mod portal zip, built by .github/workflows/release.yml)
dist/

# OS / editor
.DS_Store
*.swp

# Local dev dedicated-server write dir (saves, config, logs)
server/
