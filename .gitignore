# Secrets / local config (keep .env.example as the template)
.env
bridge/.env

# Mod-written runtime config (regenerated by the mod at startup)
config.json
bridge/config.json

# Transcripts / logs
*.log
chat.log
bridge/transcript*.txt

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS / editor
.DS_Store
*.swp
